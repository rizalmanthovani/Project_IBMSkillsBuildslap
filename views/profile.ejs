<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profil Saya - BarberLux</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="page-wrapper">
        <%- include('partials/sidebar', { activePage: 'profile', user: user }) %>

        <!-- Main Content -->
        <main class="main-content">
            <div class="container-fluid">
                <h1 class="mb-4">Profil Saya</h1>
                <div class="card">
                    <div class="card-body p-4">
                        <div class="text-center mb-4">
                            <i class="bi bi-person-circle" style="font-size: 80px; color: var(--accent-color);"></i>
                        </div>
                        <div class="mb-3">
                            <label class="form-label text-white-50">Username</label>
                            <p class="fs-5">@<%= user.username %></p>
                        </div>
                        <div class="mb-3">
                            <label class="form-label text-white-50">Nama Lengkap</label>
                            <p class="fs-5"><%= user.name %></p>
                        </div>
                        <div class="mb-3">
                            <label class="form-label text-white-50">Nomor WhatsApp</label>
                            <p class="fs-5"><%= user.whatsapp %></p>
                        </div>
                        <hr>
                        <div class="mb-3">
                            <label class="form-label text-white-50">Status Langganan AI Stylist</label>
                            <% 
                                const isSubscribed = user.ai_subscription_status === 'active' && new Date(user.ai_subscription_expires_at) > new Date();
                                const expiryDate = user.ai_subscription_expires_at ? new Date(user.ai_subscription_expires_at).toLocaleDateString('id-ID', { day: 'numeric', month: 'long', year: 'numeric' }) : '';
                            %>
                            <% if (isSubscribed) { %>
                                <div class="d-flex align-items-center">
                                    <span class="badge bg-success fs-6 me-2">Aktif</span>
                                    <p class="fs-6 mb-0">Berlaku hingga <%= expiryDate %></p>
                                </div>
                            <% } else { %>
                                <div class="d-flex align-items-center justify-content-between">
                                    <span class="badge bg-secondary fs-6">Tidak Aktif</span>
                                    <a href="/subscribe" class="btn btn-sm btn-primary fw-bold">Upgrade ke Premium</a>
                                </div>
                            <% } %>
                        </div>
                        <p class="text-center text-white-50 small">Fitur untuk mengubah profil akan segera hadir.</p>
                    </div>
                </div>
            </div>
        </main>
    </div>
</body>
</html>
<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - BarberLux</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        :root { --bs-body-bg: #121212; --bs-body-color: #e0e0e0; --accent-color: #fca311; }
        body { font-family: 'Poppins', sans-serif; background-color: var(--bs-body-bg); color: var(--bs-body-color); display: flex; align-items: center; justify-content: center; min-height: 100vh; }
        .card { background-color: #1c1c1e; border: 1px solid #3a3a3c; border-radius: 1rem; width: 100%; max-width: 400px; }
        .card-header { background-color: var(--accent-color); color: #121212; border-bottom: none; border-top-left-radius: 1rem; border-top-right-radius: 1rem; }
        .form-control { background-color: #2c2c2e; color: var(--bs-body-color); border: 1px solid #3a3a3c; }
        .form-control:focus { background-color: #2c2c2e; color: var(--bs-body-color); border-color: var(--accent-color); box-shadow: 0 0 0 0.25rem rgba(252, 163, 17, 0.25); }
        .btn-primary { background-color: var(--accent-color); border-color: var(--accent-color); color: #121212; font-weight: 600; }
    </style>
</head>
<body>
    <div class="card shadow">
        <div class="card-header text-center py-3">
            <h2 class="fw-bold mb-0">LOGIN</h2>
        </div>
        <div class="card-body p-4">
            <% if (error) { %>
                <div class="alert alert-danger"><i class="bi bi-exclamation-triangle-fill me-2"></i><%= error %></div>
            <% } %>
            <% if (success) { %>
                <div class="alert alert-success"><i class="bi bi-check-circle-fill me-2"></i><%= success %></div>
            <% } %>
            <p class="text-center text-white-50">Masukkan username dan password Anda.</p>
            <form action="/login" method="POST">
                <div class="mb-3">
                    <label for="identifier" class="form-label">Username atau No. WhatsApp</label>
                    <div class="input-group">
                        <span class="input-group-text" style="background-color: #2c2c2e; border-color: #3a3a3c; color: var(--accent-color);"><i class="bi bi-person-badge-fill"></i></span>
                        <input type="text" class="form-control" id="identifier" name="identifier" placeholder="Username atau No. WhatsApp Anda" required autocomplete="username">
                    </div>
                </div>
                <div class="mb-3">
                    <label for="password" class="form-label">Password</label>
                    <div class="input-group">
                        <span class="input-group-text" style="background-color: #2c2c2e; border-color: #3a3a3c; color: var(--accent-color);"><i class="bi bi-key-fill"></i></span>
                        <input type="password" class="form-control" id="password" name="password" placeholder="Password Anda" required autocomplete="current-password">
                        <span class="input-group-text toggle-password" style="cursor: pointer; background-color: #2c2c2e; border-color: #3a3a3c;">
                            <i class="bi bi-eye-slash-fill" style="color: var(--accent-color);"></i>
                        </span>
                    </div>
                </div>
                <div class="d-flex justify-content-end mb-3">
                    <a href="/forgot-password" class="text-white-50 text-decoration-none small">Lupa Password?</a>
                </div>
                <div class="d-grid">
                    <button type="submit" class="btn btn-primary">Login</button>
                </div>
            </form>
            <div class="text-center mt-3">
                <a href="/register" class="text-white-50 text-decoration-none">Belum punya akun? Daftar</a>
            </div>
        </div>
    </div>

    <!-- Script untuk toggle password visibility -->
    <script src="/js/auth.js"></script>
</body>
</html>

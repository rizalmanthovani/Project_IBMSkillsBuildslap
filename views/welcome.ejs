<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Selamat Datang di BarberLux</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        :root { --bs-body-bg: #121212; --bs-body-color: #e0e0e0; --accent-color: #fca311; }
        body { font-family: 'Poppins', sans-serif; background-color: var(--bs-body-bg); color: var(--bs-body-color); display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: 100vh; }
        .welcome-container { text-align: center; }
        .logo { font-weight: 700; font-size: 3rem; color: var(--accent-color); }
        .tagline { font-size: 1.25rem; color: #a0a0a0; margin-bottom: 3rem; }
        .btn-group-lg .btn { padding: 0.75rem 2rem; font-size: 1.25rem; font-weight: 600; }
        .btn-primary { background-color: var(--accent-color); border-color: var(--accent-color); color: #121212; }
        .btn-outline-light { border-color: #4a4a4c; color: #e0e0e0; }
        .btn-outline-light:hover { background-color: #2c2c2e; border-color: #4a4a4c; }
    </style>
</head>
<body>
    <div class="welcome-container">
        <h1 class="logo">BarberLux</h1>
        <p class="tagline">Booking Keren, Gaya Maksimal.</p>
        <div class="btn-group-lg" role="group">
            <a href="/login" class="btn btn-primary">Login</a>
            <a href="/register" class="btn btn-outline-light">Daftar</a>
        </div>
    </div>
</body>
</html>